Paraveda CRM v3.66 — نشر على الهوست (⚠️ index.html + api.php بجوج)
================================================================
1) public_html/index.html   → فـ public_html/ (بدّل القديم)
2) public_html/api.php      → فـ public_html/ (بدّل القديم — v3.66 ضروري:
                              هو اللي كيمنع أي تعديل يتغيّر من نسخة قديمة)
3) crm_data.json: ما فيه والو تبدّل فهاد النسخة — اللي عندك خليه كيف ما هو.
   (إلا بغيتي غير تمسح الكاش ديال جميع المتصفحات دفعة وحدة، شوف INSTALL.txt)
4) فـ المتصفح: Ctrl+F5.
   أي متصفح بقا محمّل نسخة قديمة: كيبان ليه شريط أحمر فالتحت
   «دير Ctrl+F5»، ومرة وحدة كيعاود يحمّل النسخة الجديدة بوحدو.

🔒 شنو تبدّل فـ v3.66 (الخلاصة):
   - أي تعديل درتيه (التاريخ، الثمن، Statut، المدينة...) ما كيتبدلش
     من عند متصفح آخر عندو نسخة قديمة.
   - الاستيراد من Excel ولاّ كيقرا عمود «📅 التاريخ» ديال الملف
     (قبل كان كيفرض تاريخ اليوم على كلشي).
دخول الأدمين: admin@paraveda.ma
